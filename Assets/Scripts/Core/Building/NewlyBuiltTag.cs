using Unity.Entities;

/// <summary>
/// Тег-компонент, который временно добавляется к сущности,
/// когда она только что была построена (инстанциирована) игроком.
/// Системы инициализации могут использовать этот тег для выполнения
/// одноразовых действий над новыми объектами, после чего тег удаляется.
/// </summary>
public struct NewlyBuiltTag : IComponentData { }